---
'@ww/data-grid': minor
'@ww/widgets': patch
---

Add `@ww/data-grid` as a controlled, feature-first dense admin/business table system package.

Update widget layer docs to point future `DataTableWidget` composition at `@ww/data-grid`.
