---
'@ww/widgets': minor
'@ww/data-grid': patch
---

Add `DataTableWidget` as a reusable widget-layer surface above `@ww/data-grid`.

Add composition-friendly `showToolbar`, `showBulkActions`, and `showColumnVisibility` controls to `UiDataGrid` for widget and app embedding.
