---
'@ww/themes': minor
---

Add the `belovodye` theme with a public `theme-belovodye.css` export and theme registry support.

- add `belovodye` to the typed theme registry and `setTheme()` flow
- export `@ww/themes/theme-belovodye.css`
- keep scoped subtree themes and theme-aware overlays working with the new theme
