---
'@ww/tokens': minor
'@ww/themes': minor
'@ww/charts-apex': minor
'@ww/docs': patch
'@ww/playground': patch
---

Add `@ww/charts-apex` as an optional ApexCharts adapter package with theme-aware defaults and production-ready chart states.

- add `UiApexChart` with SSR-safe client-only vendor mounting
- add chart-specific token and theme mappings for palettes, axes, tooltip, and no-data states
- add Storybook and playground coverage for charts, theming, ThemeType, responsive behavior, and states
